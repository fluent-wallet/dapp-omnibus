// import { fetchChain, type Params } from '@cfx-kit/dapp-utils/src/fetch';
// import { createMulticallContract } from '@cfx-kit/dapp-utils/src/contract';

// const multicallContract = createMulticallContract('0xd59149a01f910c3c448e41718134baeae55fa784');


// const queue = new Map<string, Array<Params>>();
// export const autoMulticallFetchChain = async <T>({ queueKey = 'defaultQueue', ...params }: { queueKey?: string; } & Params): Promise<T> => {
//   const promise = new Promise<T>((resolve, reject) => {  
//   });
//   const key = (queueKey || 'defaultQueue') + params.url;
//   queue.set(key, queue.get(key) || []);
  
//   return promise;
// }

export const a = () => 1;
